# Prototype backend notes

Run `npm install`, `npm run seed`, then `npm run dev` inside `server`.
The seed creates 30 demonstration procurement centres across India, 14 days of slots per centre, crop mappings, demo accounts and notification data.
Set `MONGO_URI`, `JWT_SECRET`, `PORT` and `CLIENT_URL` in `.env` for a deployment; `.env.example` is included.
